import { wrapper } from "./getMonaco";

/** 全局使用 Theme */
export const applyTheme = async (name: string) => {
    //     fetch('')
    //    wrapper.defineVSCodeTheme()
    //         monaco.editor.setTheme(name);
    //         console.log("MonacoEditor 应用样式 " + name);
};
